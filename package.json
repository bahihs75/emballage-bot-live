{
  "name": "emballage-bot-live",
  "version": "2.0.0",
  "private": true,
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "check": "node --check index.js && node --check deployNetlify.js && node --check src/*.js && node --check netlify/functions/*.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "17.2.0"
  }
}
